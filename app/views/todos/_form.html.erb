<%= simple_form_for(@todo) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :task %>
    <%= f.association :onboarding %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
